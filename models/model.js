/* eslint-disable indent */
const CustomModel = class DataModel {
    constructor(name, wins, losses, ot, points) {
        this.name = name;
        this.wins = wins;
        this.losses = losses;
        this.ot = ot;
        this.points = points;
    }
};

module.exports = CustomModel;
